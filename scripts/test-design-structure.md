# scripts/test-design-structure.sh — contract

`scripts/test-design-structure.sh` is the structural regression guard for the `/design` skill's progressive-disclosure invariants (asserted by the iteration-1 `/skill-judge` review) AND for the Step-3a removal residue pins (issue #453, follow-up to PR #454); substantive-validation flag pin added per #661. Seven assertions: (1) `skills/design/SKILL.md` flag table has an adjacent `MANDATORY — READ ENTIRE FILE before parsing argument flags` pointer to `references/flags.md`, placed BEFORE the `## Step 0` heading (flag parsing runs before Step 0, so the MANDATORY pointer must be adjacent to the flag table — not in Step 0); (2) `skills/design/SKILL.md` Step 2a.5 contains BOTH `Do NOT load` guards for `references/dialectic-execution.md` — one guarding the `NO_CONTESTED_DECISIONS` short-circuit path, one guarding the zero-externals-guardrail path; (3) `skills/design/references/dialectic-execution.md` exists and its header section (before step 6) contains a `MANDATORY — READ ENTIRE FILE before rendering debate prompts` directive naming `dialectic-debate.md`; (4) `skills/design/references/flags.md` exists and contains the `--branch-info` 4-key literal ("All 4 keys are required") AND the `--step-prefix` `` `::` delimiter `` literal; (5) the entire `skills/design/` tree (SKILL.md and `references/**`) contains zero matches for the Step-3a removal residue tokens `Step 3a`, `Post-Review Confirmation`, `user-qa-happened`, `qa_happened`, `dialectic_adjudicated` — pins the cleanup that landed in PR #454 against accidental reintroduction; (6) `skills/design/SKILL.md`'s Step 3 "all reviewers report no in-scope issues" branch points forward to Step 3b on the `auto_mode=true` arm (literal match `or Step 3b if auto_mode=true` with backticks around the flag name), AND the Step 3.5 auto-mode branch contains the literal `and proceed to Step 3b` — confirms the post-removal forward-pointers stay aimed at Step 3b rather than regressing to Step 3a. (7) substantive-validation flag pin (#661) — at least one `skills/design/references/plan-review.md` line carries `collect-reviewer-results.sh`, `--timeout 1860`, `--substantive-validation`, AND `--validation-mode` together; line-scoped via grep pipeline so a future edit that splits the plan-review collector invocation across lines or drops either flag fails closed under `set -o pipefail`. Anchors the issue #661 contract that banner-only plan-reviewer output is rejected as `STATUS=NOT_SUBSTANTIVE` rather than passing as `STATUS=OK`. Wired into `make lint` via the `test-design-structure` target. The test script is added to `agent-lint.toml`'s exclude list because agent-lint's dead-script rule does not follow Makefile-only references. Edits to the skill's three moved regions (flag table, Step 2a.5 extraction, `references/flags.md`, `references/dialectic-execution.md` header) must stay in sync with the harness's literal assertions, AND any edit to `skills/design/SKILL.md`'s Step 3 / Step 3.5 forward-pointer phrasing (Check 6) or to the design-tree token-absence invariants (Check 5) must update the corresponding harness check in lockstep.
