# scripts/test-loop-improve-skill-driver.sh — contract

`scripts/test-loop-improve-skill-driver.sh` is the structural + behavioral regression harness for `skills/loop-improve-skill/scripts/driver.sh` — the Option B bash-driver topology for `/loop-improve-skill` introduced by umbrella #273, which replaces the prior outer→inner→three-children Skill-tool chain with a single-file bash driver that invokes each child skill (`/skill-judge`, `/design`, `/im`) as a fresh `claude -p` subprocess (halt class eliminated by construction: each child's report is its subprocess's output, so there is no post-child-return model turn that can halt). **Structural asserts** on `driver.sh` source contents: contains `parse-skill-judge-grade.sh`, `claude --version` forensic capture, `set -euo pipefail`, `verify-skill-called.sh --stdout-line '^✅ 18: cleanup'` mechanical gate, LOOP_TMPDIR `/tmp/` + `/private/tmp/` prefix-guard literals (security boundary), `'..'` rejection check (path-traversal deny), file-based prompt writes (`iter-${ITER}-judge-prompt.txt`, `iter-${ITER}-design-prompt.txt`, `iter-${ITER}-im-prompt.txt`), `redact-secrets.sh` in the gh-comment pipeline, `cleanup-tmpdir.sh` invocation, the `max iterations (10) reached` / `grade A achieved on all dimensions` / `grade A achieved after final post-iter-cap` exit-reason literals, close-out section headings (`Grade History`, `Infeasibility Justification`), and `claude -p` subprocess invocation. **Behavioral fixtures** (best-effort, stubbed `claude` and `gh` on PATH under a fixture skill dir): grade-A short-circuit at iter 1, no_plan infeasibility exit, `/im` verification-failed infeasibility exit. Wired into `make lint` via the `test-loop-improve-skill-driver` target; added to `agent-lint.toml`'s exclude list (Makefile-only reference). Edits to `driver.sh` must stay in sync with the structural assertions in this harness — when a contract token literal changes, update the harness in the same PR. The prior `/loop-improve-skill-iter` inner skill and its continuation harness (`scripts/test-loop-improve-skill-continuation.sh`, which asserted the pre-#273 split-skill sentinel-ledger topology) have been retired.
