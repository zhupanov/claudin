# scripts/test-loop-improve-skill-skill-md.sh — contract

`scripts/test-loop-improve-skill-skill-md.sh` is the structural regression harness for `skills/loop-improve-skill/SKILL.md` (closes #291 — the live-streaming pattern: background Bash launch + Monitor attach, visible log-path emission, filter-regex parity with driver.sh breadcrumb helpers). Companion to `scripts/test-loop-improve-skill-driver.sh`: the driver harness pins `driver.sh` contract tokens; this harness pins the SKILL.md contract tokens introduced by #291. Six assertions (A–F): (A) frontmatter `allowed-tools` contains both `Bash` and `Monitor` word tokens; (B) SKILL.md body references `LOOP_DRIVER_LOG_FILE` env-overridable default AND contains the `/tmp/*|/private/tmp/*` case-arm validation literal (security boundary — must stay in parity with the driver's LOOP_TMPDIR prefix guard); (C) at least one pre-launch visibility line `📄 Full driver log: <path>` AND at least one completion `📄 Full driver log (retained): <path>` line (the user-facing contract from #291); (D) both `run_in_background: true` (background Bash launch directive) and `persistent: true` (Monitor persistence directive) literals present; (E) the exact filter literal ``tail -F "$LOG_FILE" | grep --line-buffered -E '^(✅|> \*\*🔶|\*\*⚠)'`` present byte-verbatim (the issue's explicit acceptance criterion; `$LOG_FILE` is double-quoted for whitespace tolerance in user-supplied `LOOP_DRIVER_LOG_FILE` overrides); (F) filter-regex parity with `driver.sh` breadcrumb helpers — for each of the three filter alternatives (`✅`, `> \*\*🔶`, `\*\*⚠`), `driver.sh` MUST contain a `printf` line whose format string begins with the matching prefix (`breadcrumb_done` → `✅`, `breadcrumb_inprogress` → `> **🔶`, `breadcrumb_warn` → `**⚠`); a silent drift in either file would break the live stream without any CI signal — this assertion is the mechanical bridge. Wired into `make lint` via the `test-loop-improve-skill-skill-md` target; added to `agent-lint.toml`'s exclude list (Makefile-only reference). Edits to either `skills/loop-improve-skill/SKILL.md` (frontmatter, log-path emission lines, validation block, filter literal) or to `skills/loop-improve-skill/scripts/driver.sh` breadcrumb helpers must stay in sync — the harness is the mechanical enforcer.
