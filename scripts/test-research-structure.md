# scripts/test-research-structure.sh — contract

`scripts/test-research-structure.sh` is the structural regression guard for the `/research` skill's 3-reference progressive-disclosure topology (`research-phase.md` owns Step 1 body; `validation-phase.md` owns Step 2 body; `adjudication-phase.md` owns Step 2.5 body when `--adjudicate` is set), the `--scale=standard|deep` value-flag surface + the orthogonal `--quick` boolean (#418, #520), the substantive-content validator wiring (#416 Phase 3), the §1.5 reduced-diversity banner contract (#506), and the `## Filing findings as issues` numbered procedure that anchors the post-`/issue` sentinel-file mechanical check (#509). Forty-four assertion gates (the script's final PASS message says "all 44 structural invariants"; the .sh script labels logical checks 1-38 inline — Checks 1-20 are the original topology + flag pins, 21-22 are the #506 §1.5 banner, 23-26 are the #509 sentinel procedure, 27-32 are the #510 sidecar surface, 33-37 are the #519 deep-plan support, and 38a-38c are the #534 validation-phase Finalize Validation pin; #520 added Checks 9b and 12b within the Checks 1-20 range and re-keyed Checks 9, 11, 12, and 21e for the `--quick` boolean replacing the legacy `--scale=quick` value; #522 added Check 13b for the `--interactive` flag's pre-planner TTY guard, `RESEARCH_PLAN_INTERACTIVE` mental flag, and `--interactive requires --plan` usage error pins; #531 replaced Check 13's literal-substring pin with a structural completeness check that derives the expected independent-flag set from the bullet block):

(1) `skills/research/SKILL.md` exists; (2) all three of `skills/research/references/research-phase.md`, `skills/research/references/validation-phase.md`, and `skills/research/references/adjudication-phase.md` exist; (3) each reference file is named on a `MANDATORY — READ ENTIRE FILE` line in `SKILL.md` AND that same line also carries reciprocal `Do NOT load <other-reference>` guards naming **both** other references on the same line; (4) each `references/*.md` **opens with** the `**Consumer**:` / `**Contract**:` / `**When to load**:` header triplet in the first 20 lines; (5) `RESEARCH_PROMPT_BASELINE` identifier + opening body substring `"You are researching a codebase to answer this question"` appear in `research-phase.md` — the substring `RESEARCH_PROMPT` (matched by `grep -F`) is satisfied by `RESEARCH_PROMPT_BASELINE` and by the four angle prompt names (`RESEARCH_PROMPT_ARCH` / `_EDGE` / `_EXT` / `_SEC`); the body opening substring continues to match BASELINE and all four angle-prompt bodies post-#508 (which extended `/research` standard mode to angle-differentiated prompts); (6) reviewer XML wrapper tags appear in `validation-phase.md`; (7) `SKILL.md` Step 3 references `render-lane-status.sh` (#421); (8) both phase references mention `lane-status.txt` (#421); (9) `SKILL.md` documents the literal `--scale=standard|deep` flag enum (#418, #520 — `quick` removed in favor of the orthogonal `--quick` boolean); (9b) `SKILL.md` documents the `--quick` boolean flag and the hardcoded `RESEARCH_QUICK_K=3` constant (#520); (10) `research-phase.md` defines all four named angle prompt identifiers (#418); (11) `SKILL.md` contains the literal quick-mode skip breadcrumb `⏩ 2: validation — skipped (--quick)` (#418, #520 — re-keyed from `(--scale=quick)` to `(--quick)`); (12) `SKILL.md` documents abort-on-invalid for `--scale` (#418, #460, #520 — pinned via the composite literal `must be one of standard|deep (got: foo). Aborting.` so an unrelated `Aborting` elsewhere cannot satisfy the assertion spuriously); (12b) `SKILL.md` documents the legacy `--scale=quick` fatal-error migration pointer via the literal `--scale=quick has been replaced by --quick` (#520); (13) `SKILL.md`'s flag-independence statement is structurally consistent with the flag-bullet block (#418 + #424 + #510 + #518 + #520 + #522 + #531). The check anchors on the single line in `SKILL.md` beginning `Flags are independent` (must occur exactly once); the bullet-block window starts on the line after that anchor and ends at the next `^##` H2 heading (i.e., a line beginning with two hashes followed by a space) (currently `## Token telemetry and budget enforcement`). Each top-level bullet (a line beginning with `-` at column 1 followed by a space) has its leading backticked span canonicalized to `--<name>` by stripping any `=…` or `<…>` infix, so value flags (`--scale=standard|deep`, `--token-budget=<positive integer>`) and compound bullets (`--keep-sidecar` AND `--keep-sidecar=<PATH>`) collapse to the base name. A bullet is classified as **coupled** iff its body contains the literal case-sensitive substring `cross-effect` (today only `--plan`, whose bullet documents a `--scale` cross-effect); all other bullets are **independent**. The check then asserts set-equality between the independent-flag set and the backticked `--<name>` tokens on the independence-statement line, with `LC_ALL=C sort` and `LC_ALL=C comm` for deterministic cross-platform behavior. Two distinct failure messages name the specific flag(s): `MISSING` (independent flag absent from the line) and `STALE` (flag listed on the line but not present as an independent bullet — either no bullet, or a coupled bullet wrongly listed). A sanity floor of at least 4 extracted bullets catches catastrophic anchor failures. The bullet-block window is **NOT fence-aware** (the flag block currently contains no code fences); the independence statement MUST stay on a single line of markdown source. Authors of `skills/research/SKILL.md` must use the literal phrase `cross-effect` in any bullet that should be exempted from the independence statement; (13b) `SKILL.md` documents the `--interactive` boolean flag (#522) — pinned via four literals: the `[--interactive]` bracketed flag in argument-hint, the `RESEARCH_PLAN_INTERACTIVE=true` mental-flag set in the flag-spec bullet, the `--interactive requires a TTY` pre-planner TTY-guard error literal, and the `--interactive requires --plan` usage-error literal in the resolution rule; (14) `research-phase.md` retains the standard-mode cursor/codex output filename literals (#418); (15) `validation-phase.md` retains the standard-mode cursor/codex validation filename literals (#418); (16) both phase references invoke `collect-reviewer-results.sh` with `--substantive-validation` (#416 Phase 3 + #446) — `research-phase.md` is pinned per-scale-block: extraction first narrows to the `## 1.4 — Wait and Validate Research Outputs` window, then within that window scopes to each of `### Standard (RESEARCH_SCALE=standard, default)` and `### Deep (RESEARCH_SCALE=deep)` separately (each subsection extraction stops at the next `^###`), so neither the Step 1.3 launch sections nor Standard ↔ Deep substitution can satisfy the pin; `validation-phase.md` keeps a single whole-file pin (its collection block is scale-agnostic). Both pins anchor the regex on the literal bash-invocation prefix `${CLAUDE_PLUGIN_ROOT}/scripts/` so prose paragraphs that mention both `collect-reviewer-results.sh` and `--substantive-validation` on the same line do not satisfy the assertion — only the actual command line in the bash code fence can; (17) both phase references map `STATUS=NOT_SUBSTANTIVE` in the lane-status update bullet (#416 Phase 3); (18) `adjudication-phase.md` references both `scripts/build-research-adjudication-ballot.sh` and `scripts/run-research-adjudication.sh` (#424); (19) `SKILL.md` Step 3 `### Deep (RESEARCH_SCALE=deep)` subsection invokes `render-deep-lane-status.sh` (#451); (20) `SKILL.md` Step 3 `### Deep (RESEARCH_SCALE=deep)` subsection does NOT derive headers from session-wide `cursor_available` / `codex_available` flags (#451 anti-regression); (21) `research-phase.md` §1.5 banner preamble contains five byte-exact literals — the reduced-diversity banner sentence with `<N_FALLBACK>` and `<LANE_TOTAL>` placeholders, the standard-mode formula, the deep-mode formula with `2*` multiplier, a `lane-status.txt` reference, and a `research-report.txt` reference (#506 Check 21a); (22-24) preamble-reference pin for the three multi-tool §1.5 sub-branches (Standard `RESEARCH_PLAN=false`, Standard `RESEARCH_PLAN=true`, Deep) (#506 Checks 21b/21c/21d); (25) the `### Quick (RESEARCH_QUICK=true)` subsection of §1.5 carries TWO negative pins (must NOT contain the `"Reduced lane diversity"` banner literal AND must NOT reference the preamble) plus ONE positive pin (must retain the `"K-lane vote-merge confidence"` literal) (#506 Check 21e; #520 anchor renamed from `RESEARCH_SCALE=quick` to `RESEARCH_QUICK=true` and disclaimer wording re-keyed from `Single-lane confidence` to `K-lane vote-merge confidence`); (26) `SKILL.md` Step 3 contains the byte-stable banner phrases "Reduced lane diversity" and "model-family heterogeneity claim does not hold" (#506 Check 22); (27-30) `SKILL.md` `## Filing findings as issues` H2 section pins (#509 Checks 23-26); (31) the issue-#519 deep-plan support is pinned across five section-scoped gates (Checks 33-37); (32) the `validation-phase.md` `## Finalize Validation` section carries three sub-pins for the post-#507 revision-subagent contract (Checks 38a-38c, #534).

Wired into `make lint` via the `test-research-structure` target. The test script is added to `agent-lint.toml`'s exclude list because agent-lint's dead-script rule does not follow Makefile-only references. Edits to `skills/research/SKILL.md` Step 1 / Step 2 / Step 2.5 MANDATORY directives, to any reference file's header triplet, to the pinned literal substrings, to the `--scale` flag surface, to the `--substantive-validation` wiring / `NOT_SUBSTANTIVE` token, to the adjudication helper-script references, to the standard-branch filename literals, to the deep-mode renderer wiring (Checks 19 + 20), to the §1.5 banner literal / formulas / SKILL.md preview (Checks 21-26), to the post-/issue sentinel-file procedure (Checks 23-26), to the #510 sidecar surface (Checks 27-32, added by PR #526), to the §1.2/§1.4/§1.5 deep-mode `--plan` support (Checks 33-37, added by #519), to the `validation-phase.md` `## Finalize Validation` section's revision-subagent / atomic-rewrite / 5-marker contract (Checks 38a-38c, added by #534), to the `--quick` boolean and legacy `--scale=quick` migration pin (Checks 9b + 12b + 21e re-key, added by #520), to the `--interactive` flag pin (Check 13b, added by #522), or to the structural Check 13 set-equality logic (added by #531) must stay in sync with the harness's literal assertions.
