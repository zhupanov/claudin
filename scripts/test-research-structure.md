# scripts/test-research-structure.sh — contract

`scripts/test-research-structure.sh` is the structural regression guard for the `/research` skill's 3-reference progressive-disclosure topology (`research-phase.md` owns Step 1 body; `validation-phase.md` owns Step 2 body; `adjudication-phase.md` owns Step 2.5 body when `--adjudicate` is set), the `--scale=quick|standard|deep` value-flag surface (#418), and the substantive-content validator wiring (#416 Phase 3). Eighteen assertions: (1) `skills/research/SKILL.md` exists; (2) all three of `skills/research/references/research-phase.md`, `skills/research/references/validation-phase.md`, and `skills/research/references/adjudication-phase.md` exist; (3) each reference file is named on a `MANDATORY — READ ENTIRE FILE` line in `SKILL.md` AND that same line also carries reciprocal `Do NOT load <other-reference>` guards naming **both** other references on the same line; (4) each `references/*.md` **opens with** the `**Consumer**:` / `**Contract**:` / `**When to load**:` header triplet in the first 20 lines; (5) `RESEARCH_PROMPT` identifier + opening substring appear in `research-phase.md`; (6) reviewer XML wrapper tags appear in `validation-phase.md`; (7) `SKILL.md` Step 3 references `render-lane-status.sh` (#421); (8) both phase references mention `lane-status.txt` (#421); (9) `SKILL.md` documents the literal `--scale=quick|standard|deep` flag enum (#418); (10) `research-phase.md` defines all four named angle prompt identifiers (#418); (11) `SKILL.md` contains the literal quick-mode skip breadcrumb (#418); (12) `SKILL.md` documents abort-on-invalid for `--scale` (#418, #460 — pinned via the composite literal `must be one of quick|standard|deep (got: foo). Aborting.` so an unrelated `Aborting` elsewhere cannot satisfy the assertion spuriously); (13) `SKILL.md` explicitly states that `--debug`, `--scale`, and `--adjudicate` are independent (#418 + #424); (14) `research-phase.md` retains the standard-mode cursor/codex output filename literals (#418); (15) `validation-phase.md` retains the standard-mode cursor/codex validation filename literals (#418); (16) both phase references invoke `collect-reviewer-results.sh` with `--substantive-validation` (#416 Phase 3 + #446) — `research-phase.md` is pinned per-scale-block: extraction first narrows to the `## 1.4 — Wait and Validate Research Outputs` window, then within that window scopes to each of `### Standard (RESEARCH_SCALE=standard, default)` and `### Deep (RESEARCH_SCALE=deep)` separately (each subsection extraction stops at the next `^###`), so neither the Step 1.3 launch sections nor Standard ↔ Deep substitution can satisfy the pin; `validation-phase.md` keeps a single whole-file pin (its collection block is scale-agnostic). Both pins anchor the regex on the literal bash-invocation prefix `${CLAUDE_PLUGIN_ROOT}/scripts/` so prose paragraphs that mention both `collect-reviewer-results.sh` and `--substantive-validation` on the same line do not satisfy the assertion — only the actual command line in the bash code fence can; (17) both phase references map `STATUS=NOT_SUBSTANTIVE` in the lane-status update bullet (#416 Phase 3); (18) `adjudication-phase.md` references both `scripts/build-research-adjudication-ballot.sh` and `scripts/run-research-adjudication.sh` (#424 — byte pin for the ballot-builder + coordinator wiring; without these the Step 2.5 body would have no helper invocation and would silently no-op even when `--adjudicate` is on). Wired into `make lint` via the `test-research-structure` target. The test script is added to `agent-lint.toml`'s exclude list because agent-lint's dead-script rule does not follow Makefile-only references. Edits to `skills/research/SKILL.md` Step 1 / Step 2 / Step 2.5 MANDATORY directives, to any reference file's header triplet, to the pinned literal substrings, to the `--scale` flag surface, to the `--substantive-validation` wiring / `NOT_SUBSTANTIVE` token, to the adjudication helper-script references, or to the standard-branch filename literals must stay in sync with the harness's literal assertions.
