# scripts/test-research-structure.sh — contract

`scripts/test-research-structure.sh` is the structural regression guard for the `/research` skill's 2-reference progressive-disclosure topology (`research-phase.md` owns Step 1 body; `validation-phase.md` owns Step 2 body) and for the `--scale=quick|standard|deep` value-flag surface introduced by #418. Fifteen assertions: (1) `skills/research/SKILL.md` exists; (2) both `skills/research/references/research-phase.md` and `skills/research/references/validation-phase.md` exist; (3) each reference file is named on a `MANDATORY — READ ENTIRE FILE` line in `SKILL.md` AND that same line also carries the reciprocal `Do NOT load <other-reference>` guard (line-scoped by construction so a future edit cannot split the MANDATORY and the Do-NOT-load into different paragraphs without the harness catching the drift — prevents cross-phase context pollution when either reference is skipped); (4) each `references/*.md` **opens with** the `**Consumer**:` / `**Contract**:` / `**When to load**:` header triplet in the first 20 lines of the file (a `/research`-local tightening layered on top of the cross-skill presence check enforced by `scripts/test-references-headers.sh` — matches this sibling contract's literal "opens with" promise, so a future edit that buries the triplet mid-file is caught); (5) `RESEARCH_PROMPT` identifier and the prompt-body opening substring `You are researching a codebase to answer this question` both appear in `research-phase.md` (byte-drift detection for the behavior-bearing literal); (6) reviewer XML wrapper tags `<reviewer_research_question>` and `<reviewer_research_findings>` appear in `validation-phase.md` (pin for the prompt-injection hardening delimiters); (7) `SKILL.md` references `render-lane-status.sh` in Step 3 (#421); (8) both phase references mention `lane-status.txt` (#421 surgical phase-local rewrites); (9) `SKILL.md` documents the literal `--scale=quick|standard|deep` flag enum (#418); (10) `research-phase.md` defines all four named angle prompt identifiers `RESEARCH_PROMPT_ARCH`, `RESEARCH_PROMPT_EDGE`, `RESEARCH_PROMPT_EXT`, `RESEARCH_PROMPT_SEC` (#418 deep-mode data-bearing literals); (11) `SKILL.md` contains the literal quick-mode skip breadcrumb `⏩ 2: validation — skipped (--scale=quick)` (#418 — pinned so the gate cannot silently drop or rephrase the user-visible skip signal); (12) `SKILL.md` documents abort-on-invalid for `--scale` (literals `must be one of quick|standard|deep` and `Aborting` both required) (#418); (13) `SKILL.md` explicitly states that ``\`--debug\` and \`--scale\` are independent`` (#418 order-independence — pinned with backticks because that's how the SKILL.md sentence is rendered); (14) `research-phase.md` retains the standard-mode `cursor-research-output.txt` and `codex-research-output.txt` filename literals (#418 byte-drift guard on the `### Standard` subsection); (15) `validation-phase.md` retains the standard-mode `cursor-validation-output.txt` and `codex-validation-output.txt` filename literals (#418 byte-drift guard on the `### Standard` subsection). Wired into `make lint` via the `test-research-structure` target. The test script is added to `agent-lint.toml`'s exclude list because agent-lint's dead-script rule does not follow Makefile-only references. Edits to `skills/research/SKILL.md` Step 1 / Step 2 MANDATORY directives, to either reference file's header triplet, to the pinned literal substrings, to the `--scale` flag surface, or to the standard-branch filename literals must stay in sync with the harness's literal assertions.
