# scripts/test-research-structure.sh — contract

`scripts/test-research-structure.sh` is the structural regression guard for the `/research` skill's 5-reference progressive-disclosure topology (`research-phase.md` owns Step 1 body; `validation-phase.md` owns Step 2 body; `adjudication-phase.md` owns Step 2.5 body when `--adjudicate` is set; `citation-validation-phase.md` owns Step 2.7 body; `critique-loop-phase.md` owns Step 2.8 body), the `--scale=quick|standard|deep` value-flag surface (#418), the substantive-content validator wiring (#416 Phase 3), the §1.5 reduced-diversity banner contract (#506), and the `## Filing findings as issues` numbered procedure that anchors the post-`/issue` sentinel-file mechanical check (#509). Multiple assertion gates (the script's final PASS message is the authoritative count — currently "all 55 structural invariants hold" after #926's Check 55; prior states include "all 51" pre-#543 and "all 45" pre-#516; the .sh script labels logical checks 1-N inline matching that count — Checks 39-45 added by #516 for the citation-validation-phase.md topology, Step 2.7 block, Step 3 splice contract, validate-citations.sh + sibling .md surfaces, and the file-line-regex-lib.sh source-only library plus its consumer wiring in validate-research-output.sh; the topology check now uses per-substring grep loops (presence-not-order) rather than the prior order-bound regex pair, so minor reordering of the four `Do NOT load` clauses does not break the harness; logical checks 1-38 — Checks 1-20 are the original topology + flag pins, 21-22 are the #506 §1.5 banner, 23-26 are the #509 sentinel procedure, 27-32 are the #510 sidecar surface, 33-37 are the #519 deep-plan support, and 38a-38c are the #534 validation-phase Finalize Validation pin): (1) `skills/research/SKILL.md` exists; (2) all five of `skills/research/references/research-phase.md`, `skills/research/references/validation-phase.md`, `skills/research/references/adjudication-phase.md`, `skills/research/references/citation-validation-phase.md`, and `skills/research/references/critique-loop-phase.md` exist; (3) each reference file is named on a `MANDATORY — READ ENTIRE FILE` line in `SKILL.md` AND that same line also carries reciprocal `Do NOT load <other-reference>` guards naming all four other references on the same line; (4) each `references/*.md` **opens with** the `**Consumer**:` / `**Contract**:` / `**When to load**:` header triplet in the first 20 lines; (5) `RESEARCH_PROMPT_BASELINE` identifier + opening body substring `"You are researching a codebase to answer this question"` appear in `research-phase.md` — the substring `RESEARCH_PROMPT` (matched by `grep -F`) is satisfied by `RESEARCH_PROMPT_BASELINE` and by the four angle prompt names (`RESEARCH_PROMPT_ARCH` / `_EDGE` / `_EXT` / `_SEC`); the body opening substring continues to match BASELINE and all four angle-prompt bodies post-#508 (which extended `/research` standard mode to angle-differentiated prompts); (6) reviewer XML wrapper tags appear in `validation-phase.md`; (7) `SKILL.md` Step 3 references `render-lane-status.sh` (#421); (8) both phase references mention `lane-status.txt` (#421); (9) `SKILL.md` documents the literal `--scale=quick|standard|deep` flag enum (#418); (10) `research-phase.md` defines all four named angle prompt identifiers (#418); (11) `SKILL.md` contains the literal quick-mode skip breadcrumb (#418); (12) `SKILL.md` documents abort-on-invalid for `--scale` (#418, #460 — pinned via the composite literal `must be one of quick|standard|deep (got: foo). Aborting.` so an unrelated `Aborting` elsewhere cannot satisfy the assertion spuriously); (13) `SKILL.md`'s flag-independence statement is structurally consistent with the flag-bullet block (#418 + #424 + #510 + #518 + #531). The check anchors on the single line in `SKILL.md` beginning `Flags are independent` (must occur exactly once); the bullet-block window starts on the line after that anchor and ends at the next `^##` H2 heading (i.e., a line beginning with two hashes followed by a space) (currently `## Token telemetry and budget enforcement`). Each top-level bullet (a line beginning with `-` at column 1 followed by a space) has its leading backticked span canonicalized to `--<name>` by stripping any `=…` or `<…>` infix, so value flags (`--scale=quick|standard|deep`, `--token-budget=<positive integer>`) and compound bullets (`--keep-sidecar` AND `--keep-sidecar=<PATH>`) collapse to the base name. A bullet is classified as **coupled** iff its body contains the literal case-sensitive substring `cross-effect` (today only `--plan`, whose bullet documents a `--scale` cross-effect); all other bullets are **independent**. The check then asserts set-equality between the independent-flag set and the backticked `--<name>` tokens on the independence-statement line, with `LC_ALL=C sort` and `LC_ALL=C comm` for deterministic cross-platform behavior. Two distinct failure messages name the specific flag(s): `MISSING` (independent flag absent from the line) and `STALE` (flag listed on the line but not present as an independent bullet — either no bullet, or a coupled bullet wrongly listed). A sanity floor of at least 4 extracted bullets catches catastrophic anchor failures. The bullet-block window is **NOT fence-aware** (the flag block currently contains no code fences); the independence statement MUST stay on a single line of markdown source. Authors of `skills/research/SKILL.md` must use the literal phrase `cross-effect` in any bullet that should be exempted from the independence statement; (14) `research-phase.md` retains the standard-mode cursor/codex output filename literals (#418); (15) `validation-phase.md` retains the standard-mode cursor/codex validation filename literals (#418); (16) both phase references invoke `collect-agent-results.sh` with `--substantive-validation` (#416 Phase 3 + #446) — `research-phase.md` is pinned per-scale-block: extraction first narrows to the `## 1.4 — Wait and Validate Research Outputs` window, then within that window scopes to each of `### Standard (RESEARCH_SCALE=standard, default)` and `### Deep (RESEARCH_SCALE=deep)` separately (each subsection extraction stops at the next `^###`), so neither the Step 1.3 launch sections nor Standard ↔ Deep substitution can satisfy the pin; `validation-phase.md` keeps a single whole-file pin (its collection block is scale-agnostic). Both pins anchor the regex on the literal bash-invocation prefix `${CLAUDE_PLUGIN_ROOT}/scripts/` so prose paragraphs that mention both `collect-agent-results.sh` and `--substantive-validation` on the same line do not satisfy the assertion — only the actual command line in the bash code fence can; (17) both phase references map `STATUS=NOT_SUBSTANTIVE` in the lane-status update bullet (#416 Phase 3); (18) `adjudication-phase.md` references both `scripts/build-research-adjudication-ballot.sh` and `scripts/run-research-adjudication.sh` (#424 — byte pin for the ballot-builder + coordinator wiring; without these the Step 2.5 body would have no helper invocation and would silently no-op even when `--adjudicate` is on); (19) `SKILL.md` Step 3 `### Deep (RESEARCH_SCALE=deep)` subsection invokes `render-deep-lane-status.sh` (#451 deep-mode lane attribution — section-scoped to the actual Deep subsection so a Step 0b prose mention alone cannot satisfy the pin if the Step 3 Deep invocation regresses, per #451 review FINDING_6); (20) `SKILL.md` Step 3 `### Deep (RESEARCH_SCALE=deep)` subsection does NOT derive headers from session-wide `cursor_available` / `codex_available` flags — anti-regression check, section-scoped by a fence-aware awk extractor that toggles a `in_fence` flag on bare ``` lines and only honors H2 (`^##` followed by a single space) end-anchors outside fences, so headings inside fenced markdown template blocks (like `## Research Report` / `### Findings Summary` in the Step 3 example) do not terminate extraction prematurely. The negative grep targets specific dangerous phrases for both `cursor_available` AND `codex_available` (session-wide / throughout-the-run / orchestrator-tracks-the-session-wide patterns) rather than bare identifier names, so the deep section may still legitimately reference other parts of SKILL.md while a Codex-only reintroduction of the same bug class fails per #451 review FINDING_7; (21) `research-phase.md` §1.5 banner preamble (section-scoped via `SECTION_15_PREAMBLE`) contains five byte-exact literals bundled together — the reduced-diversity banner sentence with `<N_FALLBACK>` and `<LANE_TOTAL>` placeholders, the standard-mode formula `(RESEARCH_CURSOR_STATUS != ok) + (RESEARCH_CODEX_STATUS != ok)`, the deep-mode formula `2*(RESEARCH_CURSOR_STATUS != ok) + 2*(RESEARCH_CODEX_STATUS != ok)`, a `lane-status.txt` reference (source of truth for fallback status), and a `research-report.txt` reference (the BOTH-outputs contract — banner prepended to printed synthesis AND written to the report file) (#506 Check 21a); (22) the standard-mode planner-off subsection of §1.5 (section-scoped via `SECTION_15_STANDARD_FALSE`, which captures the `RESEARCH_PLAN=false` branch) contains the literal phrase `"Reduced-diversity banner preamble"` so readers of that branch are unambiguously redirected to the preamble for trigger / formula details (#506 Check 21b); (23) same preamble-reference pin for the standard-mode planner-on subsection (`SECTION_15_STANDARD_TRUE`, the `RESEARCH_PLAN=true` branch) (#506 Check 21c); (24) same preamble-reference pin for the `### Deep (RESEARCH_SCALE=deep)` subsection (`SECTION_15_DEEP`) (#506 Check 21d); (25) the `### Quick (RESEARCH_SCALE=quick)` subsection of §1.5 (`SECTION_15_QUICK`) carries TWO negative pins (must NOT contain the `"Reduced lane diversity"` banner literal AND must NOT reference the preamble — Quick mode uses its own `"Single-lane confidence"` disclaimer instead) plus ONE positive pin (must retain the `"Single-lane confidence"` literal) (#506 Check 21e); (26) `SKILL.md` Step 3 contains the byte-stable banner phrases "Reduced lane diversity" and "model-family heterogeneity claim does not hold" — keeps the operator-facing example surface in sync with `research-phase.md` §1.5 (#506 Check 22). The four-way edit-in-sync surfaces for the §1.5 banner contract — banner literal in `research-phase.md` §1.5 preamble, structural pins in this harness (numbered 21-25 in this prose summary, sub-numbered 21a-21e in the .sh script), reference-impl assertions in `skills/research/scripts/test-degraded-path-banner.sh`, and the fully-substituted example in `SKILL.md` Step 3 (numbered 26 here, Check 22 in the .sh script) — must remain mutually consistent; (27) `SKILL.md` `## Filing findings as issues` H2 section contains the defensive `rm -f` of the sentinel path (#509 — sentinel-leak prevention; section-scoped via a fence-aware awk extractor, parallel to `extract_deep_section`); (28) the same section invokes `/issue` with the narrow `--sentinel-file` flag carrying the sentinel path (#509 FINDING_10 — replaces the `--session-env` reader proposed in the original plan to avoid widening `/issue`'s public arbitrary-path write surface); (29) the same section invokes `verify-skill-called.sh --sentinel-file` after `/issue` returns (#509 dialectic DECISION_1 + FINDING_11 — mechanical sentinel gate, parallel to `/alias` Step 4); (30) the same section explicitly documents the fail-closed-on-any-failure intent via the literal `fail-closed` and `research-result-filing` phrases (#509 FINDING_8 — prevents future contributors from silently softening the gate to a partial-success branch); (31) the issue-#519 deep-plan support is pinned across five section-scoped gates: (a) `SKILL.md` line 48 must NOT contain the phrase "is not yet supported" (Check 33 — catches accidental re-introduction of the previous deep-mode `--plan` disable rule); (b) `SKILL.md` must contain the deep-mode resolution-rule fragment ending with the literal "full functionality" — pinned via `grep -F` over the raw bytes so the deep-mode entry remains the supported path (Check 34); (c) within `research-phase.md` §1.5 H3 "Deep (RESEARCH_SCALE=deep)", the H4 sub-branch labeled "When RESEARCH_PLAN=true" (extracted via nested awk over `SECTION_15_DEEP`) must mention the literal H3 headers "Subquestion", "Per-angle highlights", and "Cross-cutting findings" — Check 35 is section-scoped via the nested extractor to avoid false-positive matches in the existing standard-mode planner branch; (d) within `research-phase.md` §1.4 H3 "Deep (RESEARCH_SCALE=deep)", the runtime-fallback prose must name all four angle-prompt literals `RESEARCH_PROMPT_ARCH`, `RESEARCH_PROMPT_EDGE`, `RESEARCH_PROMPT_EXT`, and `RESEARCH_PROMPT_SEC` (Check 36 — pins the canonical lane→slot→angle table for runtime-timeout-fallback rehydration); (e) within `research-phase.md` §1.2 H4 "Deep (RESEARCH_SCALE=deep)", the lane-assignment table must contain the literal "Lane 5 (Claude inline)" column header (Check 37 — confirms the 5-lane shape with Claude-inline as integrator); (32) the `validation-phase.md` `## Finalize Validation` section (section-scoped via `SECTION_FINALIZE_VALIDATION`, extracted by awk from `^## Finalize Validation` to the next `^##` heading or EOF — the section is the last `^##` heading in the file) carries three sub-pins for the post-#507 revision-subagent contract: (a) the literal sentence `"Route the synthesis-revision step to a separate Claude Agent subagent"` plus the `revision-raw.txt` Write-tool capture filename (Check 38a — pins the separate-subagent shape and the on-disk handoff path between subagent and orchestrator); (b) the literals `Atomically rewrite`, `research-report.txt`, `mktemp`, and `mv` (Check 38b — pins the atomic-rewrite technique that keeps Step 3's rendering pipeline reading the revised report rather than the pre-revision one); (c) each of the 5 body markers `### Agreements`, `### Divergences`, `### Significance`, `### Architectural patterns`, `### Risks and feasibility` (Check 38c — pins FINDING_1's marker contract enumerated by REVISION_PROMPT so the revision subagent preserves the same marker structure as the original Step 1.5 synthesis). Wired into `make lint` via the `test-research-structure` target. The test script is added to `agent-lint.toml`'s exclude list because agent-lint's dead-script rule does not follow Makefile-only references. Edits to `skills/research/SKILL.md` Step 1 / Step 2 / Step 2.5 MANDATORY directives, to any reference file's header triplet, to the pinned literal substrings, to the `--scale` flag surface, to the `--substantive-validation` wiring / `NOT_SUBSTANTIVE` token, to the adjudication helper-script references, to the standard-branch filename literals, to the deep-mode renderer wiring (Checks 19 + 20), to the §1.5 banner literal / formulas / SKILL.md preview (Checks 21-26), to the post-/issue sentinel-file procedure (Checks 23-26, the #509 numbering preserved by the .sh script), to the #510 sidecar surface (Checks 27-32, added by PR #526), or to the §1.2/§1.4/§1.5 deep-mode `--plan` support (Checks 33-37, added by #519), or to the `validation-phase.md` `## Finalize Validation` section's revision-subagent / atomic-rewrite / 5-marker contract (Checks 38a-38c, added by #534) must stay in sync with the harness's literal assertions.

Check 50 (#564, supersedes the original #517 Check 50): the `skills/research/SKILL.md` "Measurable lanes" prose paragraph must enumerate every canonical token-tally slot name written by `/research` code paths via `${CLAUDE_PLUGIN_ROOT}/scripts/token-tally.sh write --lane <slot>`. The check is **paragraph-scoped** — the harness extracts the prose between the `**Measurable lanes**` opener line and the `**Unmeasurable lanes**` terminator line via anchor-bounded `awk`, then iterates the 16-element `canonical_slots` array (`planner`, `Synthesis`, `Revision`, `Code`, `Code-Sec`, `Code-Arch`, `Cursor`, `Codex`, `Cursor-Arch`, `Cursor-Edge`, `Codex-Ext`, `Codex-Sec`, `Critique-1`, `Critique-2`, `Revision-Critique-1`, `Revision-Critique-2`) asserting each appears as a backtick-quoted literal (`` `<slot>` ``) within the extracted paragraph. Backtick-wrap disambiguates prefix overlaps (e.g., bare `Code` would otherwise match a substring of `Code-Arch`); anchor-bounded extraction prevents drift in unrelated SKILL.md sections from silently satisfying the assertion. Singleton boundary assertions (`grep -c '^\*\*Measurable lanes\*\*' || true` and `grep -c '^\*\*Unmeasurable lanes\*\*' || true` each must equal exactly 1) are wrapped with `|| true` so a missing/renamed anchor surfaces via the descriptive `fail` message rather than a silent `set -e` abort. The replacement was net-neutral on the gate count — Check 50 was replaced in place, not added — so #564 left the script's final PASS message at "all 51 structural invariants hold" (subsequently bumped to 52 by #543; see Check 52 below). Canonical slot names live across the four phase reference files (`research-phase.md`, `validation-phase.md`, `adjudication-phase.md`, `critique-loop-phase.md`) at each `--lane <slot>` write-call site; when a new measurable lane is added in any of those references, both the SKILL.md "Measurable lanes" paragraph AND the `canonical_slots` array in the harness must be updated (the inline comment in the harness cross-references the four authoritative files). The four critique-loop slots are documented in `references/critique-loop-phase.md` 2.8.5 as the single source of truth for those four names; Check 50 enforces the full SKILL.md inventory of which they are a subset.

Check 52 (#543): the `references/research-phase.md` `## 1.4 — Wait and Validate Research Outputs` section's `### Quick (RESEARCH_SCALE=quick)` subsection must invoke `${CLAUDE_PLUGIN_ROOT}/scripts/validate-research-output.sh` per lane, document the truncation exclusion mechanism (`: > "$LANE_FILE"`), and document the per-lane breadcrumb shape (`lane $k: NOT_SUBSTANTIVE`). The check is **subsection-scoped** via two-stage awk windowing — first to the H2 anchor matching `^## 1\.4` followed by a space (sharing the `SECTION_1_4` extractor with Check 16), then within that window to `^### Quick \(RESEARCH_SCALE=quick\)` stopping at the next `^###`. The subsection-scoping is required because the literal `### Quick (RESEARCH_SCALE=quick)` appears 3x in `research-phase.md` (Step 1.3 Quick, Step 1.4 Quick, Step 1.5 Quick) and an unscoped grep would silently pass when the wrong subsection happens to satisfy the pin. The validator pin uses an anchored regex `\$\{CLAUDE_PLUGIN_ROOT\}/scripts/validate-research-output\.sh` (parallel to Check 16's `INVOCATION_PIN`) so prose paragraphs that mention the script name without the bash-invocation prefix do not satisfy the assertion. The truncation pin uses `grep -F` on the literal `: > "$LANE_FILE"` so the chosen exclusion mechanism cannot be silently dropped by a future edit (without truncation, validator-failed-but-non-empty lane files would still be loaded by the synthesis subagent's "if a path's content is empty or unreadable, omit that lane" instruction in Step 1.5 Quick). The breadcrumb pin uses an anchored regex on the canonical operator-visible diagnostic shape so observability is preserved. Bumps the script's final PASS message from "all 51 structural invariants hold" to "all 52 structural invariants hold". Edits to the Step 1.4 Quick substantive-validation contract (validator invocation, truncation mechanism, breadcrumb shape) must stay in sync with this harness's three sub-pins.

Check 53 (#671 / supersedes #665): each of `skills/research/references/adjudication-phase.md` and `skills/research/references/citation-validation-phase.md` must (a) NOT carry the historical artifact name `research-synthesis.txt` and (b) reference the canonical tmpdir artifact `research-report.txt` at least once. #665 was a silent doc-drift regression where adjudication-phase.md referenced a nonexistent `research-synthesis.txt` artifact while the runtime code wrote `research-report.txt`. The negative pin uses `grep -Fq` on the literal substring `research-synthesis.txt` (with the `.txt` suffix) so legitimate prose like the `## Research Synthesis` markdown header in adjudication-phase.md or "research-synthesis critique" mentions in adjacent files do NOT false-positive — the bug class is specifically about the tmpdir artifact filename, not the section header. The positive pin uses `grep -Fq "research-report.txt"` to guard against a future silent mass-rename to a third name without anyone noticing. Both pins iterate over the two reference files in a single bash for-loop sharing the same canonical-name list. Edits to the artifact filename contract in either reference file must stay in sync with the runtime artifact path used by `validate-citations.sh`, the orchestrator's atomic-rewrite path in SKILL.md Step 1.5 / Step 2 Finalize Validation / Step 2.5, and the input gate in citation-validation-phase.md § 2.7.1. Bumps the script's final PASS message from "all 52 structural invariants hold" toward "all 54 structural invariants hold" (incremented further by Check 54).

Check 54 (#671 / supersedes #666): the two skip preconditions at /research Step 2.7 must be evaluated in the order **budget-abort first** (proceeds to Step 4 — the run was aborting upstream and Step 3 was already skipped), **empty-synthesis second** (proceeds to Step 3 — there is no synthesis to validate, but the run continues to render whatever Step 3 can render). #666 was a silent inversion of this order in citation-validation-phase.md § 2.7.1. The check has two sub-pins.

Check 54a — citation-validation-phase.md § 2.7.1 in-section ordering. Section-scoped via awk windowing parallel to Check 32 / Check 35 / Check 52: extract `^### 2\.7\.1 — Skip preconditions` through next `^###` heading. Singleton anchor sanity (parallel to Check 50) requires exactly one `### 2.7.1 — Skip preconditions` heading prefix in the file so a future duplicate heading cannot silently make awk pick the wrong window. The fail-message diagnostic notes the actual on-disk heading suffix `(input gate)` so contributors aren't sent searching for a heading that does not exist verbatim. Within the section, the literals `Budget-abort gate (evaluated FIRST` and `Empty-synthesis gate (evaluated SECOND` (truncated byte-pinned anchors via `grep -nF`) must each appear exactly once, and the budget-abort line number must precede the empty-synthesis line number.

Check 54b — SKILL.md Step 2.7 paragraph-scoped ordering. Anchored on the unique line `**Skip preconditions** (emitted FIRST` (Step 2.8's parallel paragraph uses `(also emitted` so the anchor disambiguates). Singleton check requires exactly one such opener line in the file. Paragraph extraction via awk: from the anchor line to the next blank line, accumulating all lines (resilient to a future reformat from single-line to multi-line layout, parallel to Check 54a's section-scoping). Within the paragraph, byte-offsets of two unique skip-breadcrumb literals — `2.7: citation-validation — skipped (--token-budget aborted upstream)` (budget) and `2.7: citation-validation — skipped (no synthesis to validate)` (empty) — must each appear exactly once, and the budget breadcrumb's byte offset must precede the empty breadcrumb's byte offset. Byte offsets are computed via bash parameter expansion (`${var%%"$pattern"*}`) rather than `awk -v` to avoid the awk `-v` shell-string-escape hazard if SKILL.md ever introduces backslashes into the paragraph. The breadcrumbs are unique per-branch identity tokens that survive paragraph reflow.

Bumps the script's final PASS message to "all 55 structural invariants hold" (Check 53 + Check 54a + Check 54b counted as a single logical Check 54, plus Check 55). Edit-in-sync surfaces for #671: the artifact-filename invariant in adjudication-phase.md / citation-validation-phase.md (Check 53), the skip-precondition wording and order in citation-validation-phase.md § 2.7.1 (Check 54a), and the skip-precondition wording and order in SKILL.md Step 2.7 (Check 54b). Edits to any of these surfaces must keep the harness's literal anchors aligned. The harness's narrow byte-pinning is intentionally brittle to wording paraphrase — the same accepted brittleness pattern as Check 12 / Check 26 / Check 38a-c — so deliberate semantic edits require coordinated harness updates, while accidental drift fails closed.

Check 55 (#926): SKILL.md Step Name Registry must list `| 3.5 | auto-issue |` row. Pins the new auto-issue step added between Step 3 (report) and Step 4 (cleanup) that creates a GitHub issue containing the research question, full report, and token spend metadata. The `--no-issue` boolean flag (default `false`) skips this step. The flag itself is auto-validated by Check 13's set-equality derivation from the flag-bullet block and the independence line — no separate Check 13 update is needed.
