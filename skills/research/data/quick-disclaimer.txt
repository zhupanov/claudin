**K-lane vote-merge confidence — no validation pass.** This research used `/research --quick`: K=3 homogeneous Claude lanes carrying an identical baseline prompt, vote-merged at synthesis via reproducible Jaccard-based clustering. Vote-merge is **not validation** (Step 2's reviewer panel was skipped) and **not cross-tool diversity** (all 3 lanes are Claude — correlated errors from shared training data, shared inductive biases, and shared prompt-context interpretation remain). Treat agreed claims (≥2/3 lanes concur) as moderately higher-confidence than single-lane output, but verify before acting on consequential decisions. For full cross-tool validation, rerun with `--scale=standard` or `--scale=deep`.
